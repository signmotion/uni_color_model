name: uni_color_model
description: >-
  A color structure for represent any color: model (CMYK, RGB, HSL, etc.),
  alpha, depth per channel, view, etc. Сan transform the structures.
repository: https://github.com/signmotion/uni_color_model
topics:
  - color
  - model
  - transform
  - image
  - palette

# Format:
#   version: major.minor.patch
# You can calculate the number version code by this formula:
#   code = m[A]jor * 100 * 100 + m[I]nor * 100 + [P]atch
# Otherwise: +AAIIPP
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  collection: ">1.17.0 <=3.0.0"
  wfile: ^0.4.1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.2

screenshots:
  - description: "UniColorModel demo"
    path: images/screenshots/zeplin_palette.webp
